# Local default. CI overrides this with the SITE_URL repository variable, and the
# deploy workflow refuses to build without one — so localhost can't reach production.
VITE_SITE_URL=http://localhost:5173
